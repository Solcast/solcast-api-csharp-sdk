// <auto-generated/>
// Generated from the Solcast API OpenAPI document. Edits are overwritten on the next release.
using System;
using System.Threading.Tasks;
using Microsoft.Kiota.Abstractions.Authentication;
using Microsoft.Kiota.Http.HttpClientLibrary;
using NUnit.Framework;

namespace Solcast.Tests.Generated
{
    /// <summary>
    /// Calls every TMY endpoint that can be reached with an unmetered location, and checks
    /// the caller gets back the data they asked for.
    /// </summary>
    [TestFixture]
    public class TMYTests
    {
        private SolcastClient client = null!;

        [SetUp]
        public void Setup()
        {
            var apiKey = Environment.GetEnvironmentVariable("SOLCAST_API_KEY")
                ?? throw new InvalidOperationException(
                    "SOLCAST_API_KEY must be set to run the SDK tests. Get a key from https://toolkit.solcast.com.au/.");

            var authentication = new ApiKeyAuthenticationProvider(
                apiKey, "api_key", ApiKeyAuthenticationProvider.KeyLocation.QueryParameter);

            client = new SolcastClient(new HttpClientRequestAdapter(authentication));
        }

        /// <summary>Get the Advanced PV power estimated actuals for a Typical Meteorological Year (TMY) at a requested advanced PV power site, derived from satellite (clouds and irradiance over non-polar continental areas) and numerical weather models (other data). The TMY is calculated with data from 2007 to 2025.</summary>
        [Test]
        public async Task Given_AnUnmeteredLocation_When_GetTmyAdvancedPvPowerIsCalled_Then_ItReturnsTheDataAsked()
        {
            var response = await client.Data.Tmy.AdvancedPvPower.GetAsync(request =>
            {
                request.QueryParameters.OutputParameters = ["air_temp", "dni", "ghi"];
                request.QueryParameters.ResourceId = "ba75-e17a-7374-95ed";
            });

            Assert.That(response, Is.Not.Null);
            Assert.That(response!.EstimatedActuals, Is.Not.Null.And.Not.Empty);
            Assert.That(response.EstimatedActuals![0].PvPowerAdvanced, Is.Not.Null, "the field the call asked for");
        }

        /// <summary>Get the irradiance and weather for a Typical Meteorological Year (TMY) at a requested location, derived from satellite (clouds and irradiance over non-polar continental areas) and numerical weather models (other data). The TMY is calculated with data from 2007 to 2025.</summary>
        [Test]
        public async Task Given_AnUnmeteredLocation_When_GetTmyRadiationAndWeatherIsCalled_Then_ItReturnsTheDataAsked()
        {
            var response = await client.Data.Tmy.RadiationAndWeather.GetAsync(request =>
            {
                request.QueryParameters.OutputParameters = ["air_temp", "dni", "ghi"];
                request.QueryParameters.Latitude = -33.856784;
                request.QueryParameters.Longitude = 151.215297;
            });

            Assert.That(response, Is.Not.Null);
            Assert.That(response!.EstimatedActuals, Is.Not.Null.And.Not.Empty);
            Assert.That(response.EstimatedActuals![0].Ghi, Is.Not.Null, "the field the call asked for");
        }
    }
}
